export interface FildType {
    id?: any;
    name?: string;
    price?: number;
    description?: string;
    image?: string;
}
export interface authType {
    password?: string;
    email?: string;
}